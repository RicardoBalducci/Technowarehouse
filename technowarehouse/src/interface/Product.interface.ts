export interface Product {
  id: number;
  name: string;
  description: string;
  image: string;
  stock: number;
  proveedor: string;
  precio: number;
  imageUrl: string; // Nueva propiedad para la URL de la imagen
}

//const user =
